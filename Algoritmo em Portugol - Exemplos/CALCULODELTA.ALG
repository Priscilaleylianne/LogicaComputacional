algoritmo "CalculoDelta"
var
   a, b, c: inteiro
   delta: inteiro
inicio
   a := 1
   b := -3
   c := -10

   delta := b*b - 4*a*c
   escreval("O valor de delta é: ", delta)
fimalgoritmo
